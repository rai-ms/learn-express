# This ensures the Prisma client is generated during `npm install`
prisma-generate="npx prisma generate"

# Optional: Skip postinstall during development for faster installs
# Set to "false" in production
prisma-skip-generate=false
