# Production Environment Configuration
NODE_ENV=production
PORT=8080
DATABASE_URL="${DATABASE_URL}"  # Should be set in production environment

# Production-specific settings
LOG_LEVEL=info
CORS_ORIGIN=https://your-production-domain.com
